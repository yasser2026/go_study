系统设计：设计一个高并发微信红包系统


一、核心难点
高并发：大量用户同时抢红包，瞬时 QPS 极高
超发问题：不能多发钱、不能重复抢、不能漏抢
数据一致性：红包金额、库存、领取记录一致
过期处理：24 小时未领自动退回
二、整体架构（分层）
前端层：请求限流、防重复点击
网关层：限流、鉴权、路由
业务服务：发红包、抢红包、查询
缓存层 Redis：核心！红包库存、可抢金额、用户领取状态
MQ 消息队列 Kafka/RocketMQ：削峰填谷，异步落库
数据库 MySQL：最终持久化，红包表、领取记录表
三、核心流程
1. 发红包
用户发红包，前端校验金额、个数
预扣余额（数据库事务）
Redis 存入红包信息：红包 ID、总金额、个数、剩余个数、剩余金额、过期时间
MQ 发送消息，异步创建红包记录入库
关键点：红包库存放 Redis，不直接查 DB，扛高并发
2. 抢红包（核心高并发）
校验：红包是否存在、是否过期、用户是否已抢过（Redis 判断）
Redis 扣减库存 + 金额（Lua 脚本保证原子性，防止超发、并发问题）
抢到后，发送 MQ，异步执行：
给用户加余额
插入领取记录
没抢到直接返回
为什么用 Lua？Redis 单线程，Lua 脚本保证：判断剩余 → 扣减，原子操作，不超发
3. 红包过期自动退回
Redis 设置过期时间，过期触发回调
MQ 定时任务扫描未领完红包，剩余金额退回发红包人账户
四、防超发、防重复、高并发方案
Redis+Lua 脚本：保证抢红包原子性，防止超发、并发问题
用户领取状态存入 Redis：一个用户只能抢一次
MQ 异步落库：削峰，避免大量写 DB 压垮数据库
多级限流：网关限流、接口限流、用户频率限制
红包预分配（优化）：发红包时直接把每个红包金额预先生成存在 Redis，抢的时候直接拿，更快
五、你面试直接说的精简版（没用过版）
我没实际做过，但了解高并发红包系统的设计思路：
整体采用Redis + MQ + MySQL架构。
发红包时预扣余额，红包库存、剩余个数存 Redis；
抢红包用Redis Lua 脚本原子扣减，防止超发；
通过 MQ 异步更新数据库，削峰填谷；
同时做限流、用户防重复领取、红包过期自动退回，保证高并发和数据一致。
要不要我把这道题整理成30 秒口语版


哈哈确实，面试版讲得细、全，听着很复杂，我给你整一套最简单、大白话版，直接背，不用记细节：
极简一句话版本（面试直接说）
高并发红包系统核心就三点：
抢红包不走数据库，全部放 Redis，用 Lua 脚本保证不超发、不重复抢；
抢到之后用 MQ 异步落库，减轻数据库压力；
加上限流、过期自动退钱就行。
超口语版（你直接照着说）
我没实际做过，大概思路就是：
红包数据都放 Redis 扛并发，抢的时候用 Redis 原子操作扣库存，避免多发。
抢到了不直接写数据库，用消息队列异步处理，减轻压力。
再加限流、防重复领取、超时退回，保证高并发和不出错。
就这，够了，面试官一听就懂，还不啰嗦。

chan 容量有限。导致oom
kafka 性能好，但是需要考虑消息丢失问题


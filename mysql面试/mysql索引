1. 索引是什么？
索引 = 排好序的快速查找数据结构
目的：不用全表扫描，快速定位数据
2. InnoDB 索引本质：B+ 树
为什么用 B+ 树？
层级少（3 层就能存 2000 万数据）
叶子节点是有序链表，范围查询极快
所有数据存在叶子节点

b+树 + 双向链表
3. 聚簇索引 vs 非聚簇索引
（1）聚簇索引（主键索引）
叶子节点直接存整行数据
每张表有且只有一个
默认就是主键
（2）二级索引（普通索引）
叶子节点存主键值
查数据需要回表：先查二级索引拿到主键 → 再查聚簇索引拿数据
4. 最左前缀原则（联合索引必考）
联合索引 (a,b,c)
能命中：
a
a,b
a,b,c
不能命中：
b
b,c
c
a,c
必须从左边第一个开始连续匹配，断了就不行。
5. 覆盖索引
查询的字段刚好在索引里，不需要回表
速度极快！

因为数据存在叶子节点中，所以覆盖索引速度极快！
二、慢查询优化 10 条万能法则（工作必用）
1. 表数据大？必须加索引
where 字段
order by
group by
join 关联字段
2. 联合索引按最左前缀建
where a=? and b=? order by c
建 (a,b,c)
3. 避免索引失效（高频）
这些会导致索引失效：
where id + 1 = 10 运算
where name like '%张三' 左边 %
where age != 20
where a is null (没设置允许 null 会失效)
where str = 123 字符串隐式转换
or 连接非索引字段
4. 大 offset 慢查询优化
慢：
sql
select * from t limit 100000, 10
快：
sql
select * from t where id > 100000 limit 10
5. 禁止 select *
只查需要的字段，更容易覆盖索引，减少 IO。
6. 小表驱动大表
sql
select * from 小表 where id in (select id from 大表)
7. order by /group by 尽量用索引排序
避免 filesort（文件排序，非常慢）
8. 尽量避免 join 太多表
MySQL 不适合超多表关联，业务能合并就合并。
9. 大批量数据用批量插入
sql
insert into t values (1),(2),(3)
10. 开启慢查询日志
sql
slow_query_log = 1
long_query_time = 1
抓出超过 1s 的 SQL 重点优化。
三、面试 30 秒总结（背这个）
MySQL InnoDB 索引底层是 B+ 树，主键索引是聚簇索引，叶子节点存整行数据；普通索引叶子节点存主键，需要回表。
联合索引遵循最左前缀原则。
慢查询优化核心：** 加合适索引、避免索引失效、避免回表、避免大 offset、避免 select ***、利用覆盖索引。
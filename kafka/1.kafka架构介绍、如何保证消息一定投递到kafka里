虽然没用过消息队列，但是理解组件

/*
核心组件
Producer（生产者）：发送消息到 Broker，支持分区发送、批量发送、压缩。
Broker（服务节点）：Kafka 服务器，一个集群由多个 Broker 组成。
Topic（主题）：消息分类，逻辑概念，生产者发消息到 Topic，消费者从 Topic 消费。
Partition（分区）：Topic 的物理分片，一个 Topic 分多个分区，分布在不同 Broker，实现水平扩展、高可用、并发。
Replica（副本）：每个分区有多个副本：
Leader 副本：读写主副本，所有读写走 Leader
Follower 副本：从 Leader 同步数据，故障时切换为 Leader
ISR（In‑Sync Replica）：与 Leader 保持同步的副本集合，只有 ISR 内副本可参与选举。
Consumer（消费者）：消费消息，Consumer Group 消费者组：组内多个消费者，一个分区只能被组内一个消费者消费，实现负载均衡。
Zookeeper：管理 Broker、分区副本选举、消费位移、集群元数据。
*/
Kafka 架构
Kafka 由生产者、Broker、Topic、Partition、副本、消费者组、ZK 组成。

Topic 分多个分区，分区有 Leader 和 Follower 副本，ISR 维护同步副本；读写走 Leader，
消费者组实现负载均衡。

保证消息一定投递

acks=all，ISR 所有副本同步成功才返回 acks 表示：Leader 收到消息后，多少个副本同步完成才返回成功
开启重试 retries
开启幂等生产者防重复
必要时开启事务保证原子性

配置多副本、最小同步副本数
同步发送或异步回调确认结果
要不要我帮你整理成超精简面试背诵版？
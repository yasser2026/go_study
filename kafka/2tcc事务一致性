TCC 分为 Try、Confirm、Cancel 三个阶段，手动控制资源锁定与回滚。
其他最终一致性方案还有：本地消息表、RocketMQ 事务消息、SAGA、最大努力通知。
需要我给你一句 **“项目没实际用过，只了解理论”** 的口语话术吗？


1. TCC 是啥？
TCC 就是手动实现的分布式事务，分三步：
Try：先占坑、冻结资源（比如冻结库存、冻结余额）
Confirm：全部没问题，真正扣钱、扣库存
Cancel：中间任何一步失败，全部取消、释放资源
特点：代码写起来麻烦、侵入业务、性能一般，但不用中间件。
2. 其他最终一致性方案（通俗版）
最终一致性：不要求瞬间同时成功，过一会数据一致就行
本地消息表
自己库建一张消息表，业务 + 消息在同一个本地事务提交，再用 MQ 通知别人，失败重试。
RocketMQ 事务消息
MQ 帮你控制：先发半消息，本地事务成功才真正发出去。
SAGA
长流程，每个服务做自己的事，失败就反向补偿（做撤销操作）。
最大努力通知
比如发通知、回调，不停重试，接受一点点延迟。
3. 你面试直接背这句（最简单）
TCC 分为 Try、Confirm、Cancel 三个阶段，手动控制资源锁定与回滚。
其他最终一致性方案还有：本地消息表、RocketMQ 事务消息、SAGA、最大努力通知。
需要我给你一句 **“项目没实际用过，只了解理论”** 的口语话术吗？
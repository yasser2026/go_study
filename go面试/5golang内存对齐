一、Go 内存对齐 3 条核心规则（背这 3 条就够）
规则 1：成员变量对齐
每个字段的偏移量（offset）必须是 自身类型大小 的整数倍
bool/int8/uint8 → 1 字节
int16/uint16 → 2 字节
int32/float32 → 4 字节
int64/float64 → 8 字节
指针（在 64 位系统）→ 8 字节
规则 2：整体对齐
整个结构体最终大小 = 最大成员字段大小的整数倍
规则 3：空结构体
struct{} 大小 0 字节，不占空间，但如果是最后一个字段会对齐占位 1 字节（极少考）
二、64 位系统默认对齐系数
64 位系统：对齐系数 = 8
32 位系统：对齐系数 = 4
三、超级经典面试题（必看）
例子 1：字段乱序
go
运行
type A struct {
    a bool   // 1
    b int64  // 8
    c bool   // 1
}
大小 = 24 字节
因为要对齐，中间补了很多空洞！
例子 2：字段按大小排序（优化后）
go
运行
type B struct {
    b int64  // 8
    a bool   // 1
    c bool   // 1
}
大小 = 16 字节
为什么差 8 个字节？
因为内存对齐会自动填充空白字节（padding）。
四、Go 内存对齐图解（面试能说出来就满分）
以结构体 A 为例：
plaintext
a bool 1 字节 → 后面补 7 字节（为了对齐 int64）
b int64 8 字节
c bool 1 字节 → 最后补 7 字节（整体对齐 8）
总：1+7+8+1+7 = 24
结构体 B：
plaintext
b int64 8
a bool 1
c bool 1
最后补 6
总：8+1+1+6 = 16
五、面试背诵版（30 秒）
Go 有内存对齐，和 C 规则一致。
每个成员偏移量必须是自身大小的整数倍。
结构体总大小必须是最大字段大小的整数倍。
字段顺序会影响结构体大小，小字段放一起可以节省内存。
空结构体占 0 字节，但末尾会占位。
六、最常问的一句话
问：为什么要内存对齐？
答：CPU 读取对齐的数据只需一次访存，未对齐的数据需要多次拼接，效率低；部分架构直接 panic
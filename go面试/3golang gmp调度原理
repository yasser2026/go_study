四、一句话总结（面试直接背）
GMP 是 Go 的 M:N 用户态调度器：G 是轻量级任务，M 是 内核OS 执行线程，P 是带本地队列的逻辑处理器；P 管理 G 队列，M 绑定 P 执行 
G；通过本地队列、全局队列、工作窃取、抢占调度，实现百万级协程低成本、高效率并发，充分利用多核 CPU。
要不要我把以上内容浓缩成一页面试背诵版（10 条以内），方便你快速记忆？


GMP M:N的用户态调度器
G: 轻量级的任务  

P：逻辑处理器，绑定 M 才能运行 G，数量默认等于 CPU 核数。  +（本地队列 + 全局队列）

M: 内核线程，真正负责执行代码。  

一般g的最大数量是10000
p最大数量是GOMAXPROCS 就是逻辑cpu数量


参考 11 篇资料
面试背诵版（10 条以内）

Go GMP 调度 面试精简背诵版（9 条）
G：goroutine，轻量级协程，执行任务，数量百万级，栈小开销低。
M：操作系统内核线程，真正负责执行代码。
P：逻辑处理器，绑定 M 才能运行 G，数量默认等于 CPU 核数。
每个 P 自带本地无锁队列，优先存放新建协程，效率最高。
本地队列满，协程放入全局队列，全局队列存在锁竞争。
协程发生系统调用阻塞，M 带着 G 阻塞，P 解绑流转新 M 继续干活。
P 无任务时执行工作窃取，偷其他 P 一半协程执行。
Go1.14 后实现基于信号抢占调度，单个协程最长运行 10ms 让出 CPU。
整体是M:N 用户态调度，不依赖内核切换，并发性能极强。

https://www.bilibili.com/video/BV19r4y1w7Nx/?spm_id_from=333.337.search-card.all.click&vd_source=fda8e86c7c2847beb72917e5344366dc